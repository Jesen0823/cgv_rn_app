import * as React from 'react';
import { Text } from 'react-native';

const CitySelectScreen = ({location = '上海市', navigation}) => {
    return (
        <Text>CitySelectScreen </Text>
    );

};

export default CitySelectScreen;