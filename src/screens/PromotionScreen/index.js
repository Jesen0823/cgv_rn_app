import * as React from 'react';
import { Text } from 'react-native';

const PromotionScreen = ({navigation: {navigate}}) => {
return (
        <Text>MineScreen </Text>
    );
};

export default PromotionScreen;